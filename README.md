# Distributed-Rate-Limiter-Spring-Boot-Core-Java-
I implemented a thread-safe distributed rate limiter using Token Bucket algorithm. It supports both global and per-user limits using ConcurrentHashMap and synchronized refill logic. I also designed it to scale with Redis for distributed environments.
